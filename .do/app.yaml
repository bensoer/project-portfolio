name: portfolio-app
region: nyc
static_sites:
  - name: portfolio-react
    git:
      repo_clone_url: https://github.com/bensoer/project-portfolio.git
      branch: main
    build_command: "npm install && npm run build"
    output_dir: dist
    environment_slug: "node-js"
domains:
  - domain: portfolio.bensoer.com
    type: PRIMARY
    wildcard: false